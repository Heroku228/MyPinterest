import { usePin } from '@/hooks/context/usePin'

export const RenderCreatePin = () => {
	// const { pin } = usePin()
	return (
		<div>
			{/* {Object.entries(pin).map(([key, value]) => (
				<p key={key}>
					{key}:{value}
				</p>
			))} */}
		</div>
	)
}
